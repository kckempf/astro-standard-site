version: 2
updates:
  # npm dependencies
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    # Runtime deps -> fix(deps): so release-please cuts a patch release.
    # Dev deps -> chore(deps-dev): (chore doesn't trigger a release).
    commit-message:
      prefix: fix
      prefix-development: chore
      include: scope
    groups:
      # Batch dev-tooling minor/patch bumps into one PR
      dev-dependencies:
        dependency-type: development
        update-types:
          - minor
          - patch
      # Batch non-major runtime bumps; majors (e.g. @atproto/api) stay separate
      production-dependencies:
        dependency-type: production
        update-types:
          - minor
          - patch

  # GitHub Actions pins (checkout@v4, setup-node@v4, etc.)
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"
